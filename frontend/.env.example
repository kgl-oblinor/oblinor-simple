# Frontend Environment Variables

# Backend API URL
# Local development: http://localhost:4001
# Production (Railway monorepo): Leave empty for relative URLs
# Production (separate services): https://your-backend.railway.app
VITE_API_URL=http://localhost:4001

# Optional: Analytics (future implementation)
# VITE_GA_TRACKING_ID=UA-XXXXXXXXX-X
# VITE_MIXPANEL_TOKEN=your-mixpanel-token

# Optional: Feature Flags
# VITE_ENABLE_PAYMENT=false
# VITE_ENABLE_2FA=false
# VITE_ENABLE_EMAIL_NOTIFICATIONS=false

# Optional: Sentry Error Tracking
# VITE_SENTRY_DSN=https://...@sentry.io/...

# Note: In production on Railway with monorepo setup:
# - Leave VITE_API_URL empty or set to ""
# - Frontend will use relative URLs (same domain)
# - Backend serves frontend from /dist folder