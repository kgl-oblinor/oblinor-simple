# Backend Environment Variables

# Database Connection
# Local development: postgresql://user@localhost:5432/oblinor_simple
# Railway: Automatically set via Railway PostgreSQL plugin
DATABASE_URL=postgresql://user:password@host:port/database

# JWT Secret - MUST CHANGE IN PRODUCTION
# Generate secure key: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-CHANGE-THIS

# Environment (development | production)
NODE_ENV=development

# Server Port
# Local: 4001, Railway: Automatically set via $PORT
PORT=4001

# Optional: CORS Origin (if frontend on different domain)
# CORS_ORIGIN=https://your-frontend-domain.com

# Optional: Email Service (future implementation)
# SENDGRID_API_KEY=your-sendgrid-api-key
# EMAIL_FROM=noreply@oblinor.no

# Optional: Payment Gateway (future implementation)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Optional: Monitoring (future implementation)
# SENTRY_DSN=https://...@sentry.io/...