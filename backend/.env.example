# Backend Environment Variables - Example Template
# Copy this file to .env and customize for your setup
# NOTE: Production uses Railway environment variables

# Database Connection - Use your own database for development
# For Railway production: Set via Railway dashboard environment variables
DATABASE_URL=postgresql://user:password@localhost:5432/oblinor_simple

# JWT Secret - Generate with: openssl rand -base64 32
# For production: Set via Railway dashboard environment variables  
JWT_SECRET=your-secure-jwt-secret-here

# Environment
NODE_ENV=development

# Server Port
PORT=4001

# Production deployment uses Railway environment variables:
# - DATABASE_URL (set in Railway dashboard)
# - JWT_SECRET (set in Railway dashboard) 
# - NODE_ENV=production (automatic)
# - PORT (automatic)