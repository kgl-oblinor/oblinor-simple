{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd backend && npm install --no-cache --force && npm run build && cd ../frontend && npm install --no-cache --force && VITE_API_URL='' npm run build && cp -r dist ../backend/"
  },
  "deploy": {
    "startCommand": "cd backend && NODE_ENV=production npm start",
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}